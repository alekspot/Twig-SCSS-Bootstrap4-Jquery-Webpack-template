// Функции DOM

const htmlElement = document.querySelector('h1')    // найти элемент

htmlElement.classList.add('class1') // добавить/удалить класс

htmlElement.classList.remove('class1')

htmlElement.className // узнать какие классы есть у элемента

htmlElement.append(htmlElement) // вставить внутрь html элемента html элемент